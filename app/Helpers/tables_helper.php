<?php
	function spark_drawTable($ths=[],$records=[]){
		echo spark_startTableFormatted($ths);
		foreach($records as $record){
			foreach($record as $k=>$v){
				//if($)
			}
		}/*foreach*/
		echo spark_endTable();
	}/*drawTable*/